# Task: Update Go to 1.24

**Date:** 2026-01-25
**Status:** Planning

## Problem Statement

Update the repository to use Go 1.24 everywhere it is referenced.

## Proposed Solution

Raise the Go version in `go.mod`, update the GitHub Actions workflow to use Go 1.24 and the latest `actions/setup-go`, and refresh any documentation that mentions an older Go version.

## Detailed Steps

1. [x] Update `go.mod` to `go 1.24`.
   - Files: `go.mod`
   - Changes: Bump the Go directive to 1.24.

2. [x] Update CI to use Go 1.24.
   - Files: `.github/workflows/go.yml`
   - Changes: Set `go-version: 1.24` and update `actions/setup-go` to v5.

3. [x] Refresh documentation references.
   - Files: `README.md`, `specs/spec.md` (and any other matches)
   - Changes: Replace old Go version references with 1.24.

## Testing Strategy

- [x] Unit tests: `go test ./... -v`
- [ ] Build: `go build -v .`

## Open Questions

1. Should CI also run with older Go versions for compatibility?

## Risks and Edge Cases

- Go 1.24 could introduce incompatibilities with dependencies or tooling.
- CI runner images need to support Go 1.24 across all platforms.

## Rollback Strategy

Revert the Go version changes in `go.mod` and `.github/workflows/go.yml` to the prior values.
